<h4>配送先登録/一覧</h4>
<table class="table">
  <thead>
    <% @customers.each do |customer| %>
    <tr>
      <th>郵便番号</th>
      <th>住所</th>
      <th>宛名</th>
      <thcolspan="4"></th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td></td>
      <td></td>
      <td></td>
      <td>
<%= link_to "編集する",edit_admin_customer_path(@customer.id), class: 'btn btn-success' %>
<%= link_to "削除する", admin_root_path, class: 'btn btn-danger' %>

      </td>
    </tr>
    <% end %>
  </tbody>