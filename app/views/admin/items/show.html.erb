<h2>商品詳細</h2>

  <table class="table">
  <thead>
  <tr>
  <td>商品名</td>
  <td>商品説明</td>
  <td>ジャンル</td>
  <td>税込価格（税抜価格）</td>
  <td>販売ステータス</td>
  </tr>
  </thead>
  <tbody>
  <tr>
  <td><%= @item.name %></td>
  <td><%= @item.instroduction %></td>
  <td><%= @item.genre.name %></td>
  <td><%= @item.price %></td>
  <td><% if @item.is_active %>
        販売中
      <% else %>
        販売停止中
      <% end %></td>
  </tr>
  </tbody>
  </table>
<%= link_to "編集する",edit_admin_item_path(@item.id), class: 'btn btn-success' %>
