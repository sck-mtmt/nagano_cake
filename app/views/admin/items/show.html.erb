<h2>商品詳細</h2>

  <table class="table">
  <thead>
  <tr>
  <td>商品名</td>
  <td>商品説明</td>
  <td>ジャンル</td>
  <td>税込価格（税抜価格）</td>
  <td>販売ステータス</td>
  </tr>
  </thead>
  <tbody>
  <tr>
  <td><%= @item.name %></td>
  <td><%= @item.instroduction %></td>
  <td><%= @item.genre_id %></td>
  <td><%= @item.price %></td>
  <td><%= @item.is_active %></td>
  </tr>
  </tbody>
  </table>
  <%= f.submit '編集する', class: "btn btn-success" %>
