<h4>注文履歴詳細</h4>
<table>
  <tr>
<th>購入者</th>
<td></td>

<th>注文日</th>
<td></td>

<th>配送先</th>
<td></td>

<th>支払方法</th>
<td></td>

<th>注文ステータス</th>
<td></td>

</tr>
</table>
<table class="table">
  <thead>
    <tr>
      <th>商品名</th>
      <th>単価（税込）</th>
      <th>数量</th>
      <th>小計</th>
      <th>製作ステータス</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <% total=0 %>
      <% @order_detail.each do |order_detail| %>
      <td><%= order_detail.item.name %></td>
      <td><%= order_detail.item.with_tax_price %></td>
      <td><%= order_detail.amount %></td>
      <td><%= order_detail.item.with_tax_price * cart_item.amount %></td>
      <% total += cart_item.item.with_tax_price * cart_item.amount %>
      <td><%= form_with model: order_detail, url: public_cart_item_path(cart_item.id), method: :patch do |f| %>
      <%= f.select :amount, *[1..10] %>
      <%= f.submit "変更", class: "btn btn-success" %>
      <% end %></td>
      <% end %>
    </tr>
  </tbody>
<table>
  <tr>
    <th>送料</th>
    <td><%= @order.shipping_cost %></td>
  </tr>
  <tr>
    <th>商品合計</th>
    <td><%= total %></td>
  </tr>
    <tr>
    <th>請求金額</th>
    <td><%= @order.total_paymont = total + @order.shipping_cost %></td>
  </tr>
  </table>
 <p>支払方法</p>
    <%= @order.payment_method_i18n %><br>

 <p>お届け先</p>
    〒<%= @order.postal_code %>
      <%= @order.address %><br>
      <%= @order.name %>
